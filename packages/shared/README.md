# @eros/shared

Shared utilities and types consumed by `@apps/server` and any app that needs them.

## Usage

```typescript
import { } from "@eros/shared";
```

## Scripts

```bash
bun test          # run tests (*.test.ts alongside source)
bun run typecheck # tsc --noEmit
```
